{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.Extensions.Http.DefaultHttpClientFactory": "Warning"
    }
  },
  "App": {
    "Authorization": {
      "Type": "None", // None, AccessKey
      "AuthorizationHeader": "Authorization", // HTTP header used to pass key/token
      "AccessKey1": "", // Must be set if Type is Keys, min 16 chars
      "AccessKey2": "", // Must be set if Type is Keys, min 16 chars
    },
    "Orchestrator": {
      "WorkspaceDir": "", // where to persist workflows' state. Defaults to user profile dir
    },
    "Tools": {
      //        "chunker": "https://localhost:4003",
      //        "extractor": "https://localhost:4014",
      //        "wikipedia": "https://localhost:7261",
      //        "type-chat": "http://localhost:54670",
      //        "embedding-generator": "https://localhost:5084"
    }
  },
  "AllowedHosts": "*",
}
